# -------------------------
# Application Configuration
# -------------------------

# Application name
spring.application.name=online-art-gallery

# Server Configuration
server.port=8082

# -------------------------
# MySQL Database Connection
# -------------------------
spring.datasource.url=jdbc:mysql://localhost:3306/art_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
# Set your local MySQL root password here
spring.datasource.password=root@1234
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate / JPA Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Format SQL output in logs
spring.jpa.properties.hibernate.format_sql=true

# -------------------------
# Server Configuration
# -------------------------


# -------------------------
# Logging
# -------------------------
logging.level.org.springframework=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# JWT settings
jwt.secret=change-this-to-a-very-secure-and-long-secret-key
jwt.expiration-ms=86400000

# -------------------------
# Razorpay Payment Gateway Configuration
# -------------------------
# SECURITY WARNING: DO NOT commit real credentials to version control!
# Add this file to .gitignore or use environment variables for production.
# 
# For production, set these as environment variables:
#   export RAZORPAY_KEY_ID=your_live_key_id
#   export RAZORPAY_KEY_SECRET=your_live_key_secret
# 
# Spring Boot will automatically use environment variables if they exist,
# falling back to these test credentials for local development.
# -------------------------
razorpay.key_id=${RAZORPAY_KEY_ID:rzp_test_RfBGGYJbfq65vW}
razorpay.key_secret=${RAZORPAY_KEY_SECRET:hXEKpohMP06ZJBnamIEvHR2j}
